# Distribution-only repository.
# Everything is ignored except the files whitelisted below.
# APKs are published as GitHub Release assets, not committed.

/*
!/.gitignore
!/version.json
!/lib/services/update_service.dart
